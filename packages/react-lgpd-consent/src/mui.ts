/**
 * Re-export Material UI helpers and components from the scoped package.
 *
 * This file provides a stable import path so consumers can do:
 *   import { Button, ThemeProvider } from 'react-lgpd-consent/mui'
 *
 * It proxies all exports from '@react-lgpd-consent/mui'.
 */
export * from '@react-lgpd-consent/mui'
